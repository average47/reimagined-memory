{
  "name": "cms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "setup": "docker compose logs -f wp-cli",
    "clean": "docker compose down -v"
  }
}
